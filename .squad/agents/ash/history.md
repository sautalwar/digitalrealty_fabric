# Ash — History

## Project Context

Digital Realty Microsoft Fabric. Semantic model in TMDL format (DirectLake mode). RLS with DAX filtering by USERPRINCIPALNAME(). OLS for hidden security bridge tables. Financial KPI measures: assets, budgets, variance, growth. RLS rules in security/rls-rules.dax. OneLake RBAC roles in security/onelake-roles.json. User: Saurabh.

## Learnings

(No learnings yet — new team member)
